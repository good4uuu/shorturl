name: Build and test
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '10.0.x' }
      - uses: actions/setup-node@v4
        with: { node-version: '24', cache: 'npm', cache-dependency-path: 'src/UrlShortener.Web/package-lock.json' }
      - run: dotnet test UrlShortener.sln
      - run: npm ci
        working-directory: src/UrlShortener.Web
      - run: npm run build
        working-directory: src/UrlShortener.Web